version: "3"
env:
  PGM_NAME: firefly
tasks:
  build:
    cmds:
      - cargo fmt
      - cargo build --release
  run:
    cmds:
      - target/release/$PGM_NAME
  install:
    deps: [build]
    cmds:
      - cp target/release/$PGM_NAME ~/bin
